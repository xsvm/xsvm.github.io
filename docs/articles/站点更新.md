# 2025.4.5

markdown渲染latex公式，采用：markdown-it-katex

git命令:
```
git add .
git commit -m "Update content"
git push origin main
```
# 日常更新示例
```
git add .
git commit -m "更新文章内容"
git push origin main  # 自动触发部署
```